{
  "buildCommand": "prisma generate && mkdir -p public",
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "includeFiles": "prisma/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
